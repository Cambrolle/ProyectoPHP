{% extends 'base.html.twig' %}

{% block title %}Inicio - LCDF{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-12 text-center py-5">
            <h1 class="display-4 fw-bold">Bienvenido a {{ controller_name }}</h1>
            <p class="lead text-muted">Consulta y valora las mejores competiciones del mundo</p>
            <hr class="my-4" style="max-width: 200px; margin: auto; border-top: 3px solid #000;">
        </div>
    </div>

    <div class="row justify-content-center">
        <div class="col-md-6 col-lg-4">
            <a href="{{ path('app_categories_index') }}" class="text-decoration-none text-dark">
                <div class="card shadow-sm border-0 text-center py-5 h-100 bg-white" style="transition: transform 0.3s; border-radius: 15px;">
                    <div class="card-body">
                        <div class="display-1 mb-3">🏆</div>
                        <h2 class="fw-bold">CATEGORÍAS</h2>
                        <p class="text-muted">Explora las ligas por categorías y regiones</p>
                        <span class="btn btn-outline-dark mt-3">Ver todas</span>
                    </div>
                </div>
            </a>
        </div>
    </div>

    <style>
        .card:hover { transform: translateY(-10px); }
    </style>
{% endblock %}
