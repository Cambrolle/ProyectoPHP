<nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm">
    <div class="container">
        <a class="navbar-brand fw-bold" href="{{ path('app_home') }}">⚽ La Casa Del Fútbol</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto align-items-lg-center">
                <li class="nav-item">
                    <a class="nav-link active" href="{{ path('app_home') }}">Inicio</a>
                </li>

                {% if app.user %}
                    <li class="nav-item ms-lg-3">
                        <span class="nav-link text-info">Hola, <strong>{{ app.user.username }}</strong></span>
                    </li>
                    <li class="nav-item ms-lg-2">
                        <a class="btn btn-sm btn-outline-danger" href="{{ path('app_logout') }}">Cerrar Sesión</a>
                    </li>
                {% else %}
                    <li class="nav-item ms-lg-3">
                        <a class="btn btn-sm btn-primary text-white" href="{{ path('app_login') }}">Iniciar Sesión</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
